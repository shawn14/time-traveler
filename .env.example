# Server port (Google Cloud Run will override this with PORT env variable)
PORT=3001

# Gemini API Key (required for server-side API calls)
GEMINI_API_KEY=your_gemini_api_key_here

# Node environment
NODE_ENV=production